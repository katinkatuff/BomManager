<UserControl x:Class="BomManager.Views.PartsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BomManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <WrapPanel Orientation="Horizontal" Grid.Row="0">
      <Button Content="Add" Command="{Binding AddPartCommand}" Padding="8 4" Margin="0 0 8 0"/>
      <Button Content="Edit" Command="{Binding EditPartCommand}" Padding="8 4" Margin="0 0 8 0" />
      <Button Content="Delete" Command="{Binding DeletePartCommand}" Padding="8 4" Margin="0 0 8 0"/>
    </WrapPanel>
    <DataGrid Grid.Row="1" ItemsSource="{Binding GetPartsTask.Result}" CanUserAddRows="False">
      
    </DataGrid>
  </Grid>
</UserControl>

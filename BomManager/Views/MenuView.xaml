<UserControl x:Class="BomManager.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BomManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Menu>
    <MenuItem Header="File">
      <MenuItem Header="Save" Command="{Binding SaveCommand}"/>
      <MenuItem Header="Exit" Command="{Binding ExitCommand}" />
      <Separator />
      <MenuItem Header="Add module" Command="{Binding AddModuleCommand}" />
    </MenuItem>
    <MenuItem Header="Parts">
      <MenuItem Header="Add part" Command="{Binding AddPartCommand}" />
    </MenuItem>
  </Menu>
</UserControl>
